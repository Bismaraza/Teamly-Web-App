$(function(){ $('.hamb').on('click',()=>$('.glass-nav').toggleClass('open')); $('.faq-q').on('click',function(){$(this).next().slideToggle(180)}); $('.app-search').on('input',function(){let v=this.value.toLowerCase();$('.app-card').each(function(){$(this).toggle($(this).text().toLowerCase().includes(v))})}); $('.plan-btn').on('click',function(){localStorage.setItem('selectedPlan',$(this).data('plan'));location.href='signup.html'}); const plan=localStorage.getItem('selectedPlan'); if(plan&&$('#selectedPlan').length) $('#selectedPlan').val(plan);});
function addDemoTask(){const v=$('#taskInput').val(); if(!v)return; $('#todoCol').append(`<div class="task"><span class="tag">New</span><h4>${v}</h4><p class="muted">Added from jQuery demo.</p></div>`); $('#taskInput').val('')}
